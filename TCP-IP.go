package main

import "fmt"

var server struct{
	recieve := make(chan string, 1)
	
}
func main() {

	
	/* for {
		fmt.Println("pls input command")

		var command,err = fmt.Scanln()
		_ = err

		switch command {
        case "":
            fmt.Println("1")
		}

		
	} */
}
